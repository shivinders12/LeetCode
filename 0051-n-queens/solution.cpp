
            i--;
            j--;
        }
                return false;

        {
            if(help[i][j] == "Q")
        int j = col - 1;

        while(i >= 0 && j >= 0)
       
        int i = row - 1;
        }

                return false;
            if(help[i][col] == "Q")
        for(int i = 0; i < row; i++)
        {
    {
        

    bool isvalid(vector<vector<string>>& help, int row, int col, int n)
class Solution {
public:
