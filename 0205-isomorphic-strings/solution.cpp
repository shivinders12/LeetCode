                return false;

            mp[s[i]] = t[i];
            used[t[i]] = true;
        }
        else {
            // s[i] is already mapped
            if (mp[s[i]] != t[i])
            if (used[t[i]])
            // s[i] is not mapped yet
        if (mp.find(s[i]) == mp.end()) {

    for (int i = 0; i < s.length(); i++) {

