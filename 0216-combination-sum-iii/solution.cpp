
        }

    }
            ans.pop_back();
            solve(k,k1+1,n,help,ans,sol,sum+help[i],i+1);
            ans.push_back(help[i]);
        {
            if(sum+help[i]>n || k1>k) break;

        for(int i=index;i<help.size();i++)
        } 

            sol.push_back(ans);
            return;
        if(k1==k && sum==n)
        {
        
    index)

    {   

    void solve(int k,int k1, int n, vector<int>& help,vector<int>& ans,vector<vector<int>>& sol,int sum,int 
class Solution {
public:
