
            st.push_back(digit);
        }

      
        while (k > 0) {
            st.pop_back();
            k--;
        }
            }
                k--;
                st.pop_back();
                   st.back() > digit &&
                   k > 0) {

            while (!st.empty() &&

        for (char digit : num) {

        string st;
class Solution {
public:
    string removeKdigits(string num, int k) {
