                    found = false;
                    break;
                }
            }

            if (found)
                if (temp[i + j] != goal[j]) {

            for (int j = 0; j < s.length(); j++) {
            bool found = true;
        for (int i = 0; i < s.length(); i++) {

        string temp = s + s;

