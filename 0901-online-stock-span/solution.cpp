        int x=vt.size();
        int maxcount=0;
        int count=0;
     

        for(int i=x-1;i>=0;i--)
        {
            if(vt[i]<=price)
            {
                count++;
                maxcount=max(maxcount,count); 
            }
        vt.push_back(price);
    int next(int price) {
        
    
        
    }
       

    StockSpanner() {
        stack<int> st;
        vector<int> vt;
class StockSpanner {
public:
