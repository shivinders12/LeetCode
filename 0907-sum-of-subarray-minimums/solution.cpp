        while(!st.empty())
            st.pop();

            st.push(i);
        }

            else
                left[i] = i - st.top();
            if(st.empty())
                left[i] = i + 1;
                st.pop();

        for(int i = 0; i < n; i++) {
            while(!st.empty() && arr[st.top()] >= arr[i])
        

        vector<int> left(n); right(n);
        stack<int> st;

        long long MOD = 1e9 + 7;
    int sumSubarrayMins(vector<int>& arr) {
        int n = arr.size();
public:
class Solution {
