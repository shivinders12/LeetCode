        }

            else {
                return xCenter - x2 <= radius;
            }
            }

                int dy = y1 - yCenter;

                return dx * dx + dy * dy <= radius * radius;
                return dx * dx + dy * dy <= radius * radius;
            }

            else if (yCenter <= y1) {
                int dx = xCenter - x2;
                int dx = xCenter - x2;
                int dy = yCenter - y2;

            if (yCenter >= y2) {

        else if (xCenter >= x2) {
  

        }
            }
                return x1 - xCenter <= radius;
