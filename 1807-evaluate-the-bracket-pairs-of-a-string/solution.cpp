
    
                while (st.back() != '(') {
                    key.push_back(st.back());
                    st.pop_back();
                string key;
            else {
                st.push_back(c);
            }
        for (char c : s) {

            if (c != ')') {

        }

        string st;
        
        for (auto &x : knowledge) {
            mp[x[0]] = x[1];
                }

          
                st.pop_back();

             
                reverse(key.begin(), key.end());
