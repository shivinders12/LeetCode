
            used[i] = true;

                continue;
            if (count == 0 && digits[i] == 0)
            
            
            if (used[i])
                continue;

            solve(digits, used, ans,
                  value * 10 + digits[i],
                  count + 1,check);

            used[i] = false;
        }
    }

        for (int i = 0; i < digits.size(); i++) {
        }

            return;
                check[value]=true;
                ans++;
            if (value % 2 == 0 && check[value]==false)
